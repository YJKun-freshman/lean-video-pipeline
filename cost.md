# 成本假設與決策說明

## 整體策略：全程 $0 花費

題目明寫「API key 請使用你自己申請的免費額度即可」。實測 Anthropic、OpenAI 這類主流
API 目前都必須先綁定信用卡儲值，沒有真正無門檻的免費層級，與題目原意有落差。因此本專案
在四個步驟都改選**真正免費、無需信用卡**的工具組合，最終單支影片 demo 的實際花費為 **$0**。

| 步驟 | 選用工具 | 是否需要信用卡 | 花費 |
|---|---|---|---|
| Step1 影片轉音檔 | yt-dlp | 否（本地執行） | $0 |
| Step2 音檔轉逐字稿 | faster-whisper（本地端） | 否（本地執行） | $0 |
| Step3 腳本改寫+篩選 | Google Gemini API | 否（AI Studio 申請） | $0（在免費額度內） |
| Step4 短影音生成 | edge-tts + matplotlib + moviepy | 否（皆為本地/免費服務） | $0 |

## 各步驟成本判斷細節

### Step1：影片轉音檔

- 只下載音軌（`bestaudio`），不下載影像流，減少頻寬與轉檔時間
- 快取機制：以 `video_id` 為 key，音檔已存在則直接跳過下載，避免對同一支影片重複發出
  下載請求（YouTube 有請求頻率限制，重複下載也浪費頻寬與時間）

### Step2：音檔轉逐字稿——這是最初評估中最貴的一步，也是重新設計的重點

**原始評估**：OpenAI Whisper API 依音檔長度計費，15分鐘音檔雖然單價不高，但仍是持續性
成本，且需要先儲值信用卡。

**改為**：本地端執行開源 `faster-whisper`（base 模型），完全免費、無 API 呼叫、無額度
限制。代價是需要本機算力與較長的運算時間，且模型辨識準確度略低於雲端付費模型，但對於
demo 等級用途已足夠（實測辨識結果可讀性高，僅有零星錯字如「新鮮案」應為「新青安」）。

**仍保留的成本意識機制**：即使改成本地運算不涉及金錢成本，仍保留「長音檔前處理」機制——
若音檔超過 `MAX_AUDIO_MINUTES`（預設15分鐘），會先用 ffmpeg 的 `silenceremove` 濾除
靜音段落再送進轉錄，理由是本地運算的「成本」變成時間與電力消耗，同樣值得優化，也保留了
原本設計「不把整支長影音原封不動送進下一步」的精神，供未來若切回付費 API 時直接沿用。

實測：15.8分鐘音檔前處理後仍為15.8分鐘（本次素材幾乎沒有靜音段落，前處理未大幅縮短長度，
但機制本身仍會在有需要時發揮作用）。

### Step3：逐字稿改寫為多段短影音腳本——成本控制的核心

這是題目原文點名「通常是流程中最昂貴的步驟」，本專案的因應方式：

1. **逐字稿只送一次 API**：不是每個候選段落分開呼叫 Gemini，而是把完整逐字稿一次送入，
   由 Gemini 在單次呼叫中完成「切分主題→評分→改寫→抓取圖表數據」四件事，將 API 呼叫
   次數從「N段 × 1次」壓縮成「1次」

2. **評分篩選機制**：Gemini 針對每個候選段落給出 1-10 分（依資訊密度與獨立完整性），
   程式端再依分數排序，只挑 `MAX_SEGMENTS_TO_GENERATE`（預設3）段進入 Step4。未被選中
   的段落不會再產生任何 TTS 語音或影片，避免對次要內容重複投入運算資源

   **實測誠實揭露**：本次15分鐘素材，Gemini 僅切出2-3個候選段落（曾兩次測試分別得到
   2段與3段，這是 LLM 本身推論的隨機性所致），剛好落在 `MAX_SEGMENTS_TO_GENERATE=3`
   的門檻附近，因此篩選機制沒有真正刷掉任何候選段落。若素材更長、主題更多元，篩選機制
   才會真正發揮「排除低分段落」的作用，這點在此誠實說明，非程式邏輯錯誤

3. **免費額度選擇**：改用 Google Gemini API（AI Studio 申請）取代 Anthropic API，
   前者對新用戶提供真正免費、無需信用卡的額度，符合題目「免費額度即可」的要求

### Step4：腳本生成短影音

- **只對已篩選段落生成**：TTS 語音與影片組裝都只針對 Step3 篩選出的段落執行，未入選的
  候選段落不會產生任何語音或影片檔案
- **TTS 選用 edge-tts**：微軟 Edge 瀏覽器背後的免費語音服務，不需要任何 API key、
  沒有額度限制
- **圖表程式化生成**：一律用 matplotlib 重製，符合題目「不可截圖、不建議用 Canva/Excel」
  的規則，且圖表數據由 Gemini 直接從逐字稿內容萃取真實數字（見 Step3），非人工事先
  準備或程式端寫死的假資料

## 快取機制：避免同一支影片被重複處理、重複產生費用

以 YouTube `video_id` 作為所有快取的 key，每支影片獨立存放在 `data/cache/{video_id}/`
資料夾下。每個步驟執行前都會先檢查對應的輸出檔案是否已存在：

- 已存在 → 直接讀取快取結果，印出「命中快取，跳過」，不重新執行
- 不存在 → 執行該步驟並落地儲存結果

實測驗證：對同一支影片重複執行 `download.py`，第二次執行印出「命中快取，跳過」，
未重新發出下載請求；`transcribe.py`、`rewrite.py`、`render.py` 皆採用同樣機制。

這個設計即使在全面改用免費工具、不涉及金錢成本後仍然保留，理由是：
1. 本地運算（Whisper 轉錄、影片渲染）仍消耗時間與電力，快取可大幅縮短重跑時間
2. Gemini API 雖在免費額度內，仍有「每分鐘/每日請求數」的用量限制，快取可避免不必要地
   逼近限額
3. 若未來因應面試官需求切回付費 API（例如 OpenAI Whisper 或 Anthropic API），快取機制
   可直接沿用，無需重新設計，真正做到「一次設計、多種情境適用」

## 已知限制與取捨

- **字幕同步為近似值**：字幕依句子字數比例分配顯示時間，非逐字對齊語音的精確時間戳記。
  若要做到逐字精確同步，需要額外解析 edge-tts 的 word boundary 事件並重新設計時間軸
  邏輯，考量此為 demo 等級需求、且目前的近似同步已達到「可讀、跟語音節奏大致同步」的
  效果，因此未進一步投入時間處理
- **未整合外部 AI 影片生成/Avatar 服務**：面試口頭補充提到 Avatar/LinkSync 屬加分項，
  非書面題目要求。評估市面上主流服務（HeyGen、D-ID、Synthesia 等）後，發現免費層級
  普遍附帶浮水印或嚴格額度限制，與本題「免費額度即可」及「成本意識優先於精緻度」的
  精神方向不符，因此決定不採用，將時間資源優先投入在四步驟核心流程與成本控制機制上