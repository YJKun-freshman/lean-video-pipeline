# 成本說明

## 摘要

- **現金花費：$0。** 四個步驟都使用免費、可本地執行或免費額度的工具，沒有綁定付費 API。
- **但「免費」不等於「沒有成本」**，實際付出的是：**本機運算時間**（Whisper 轉錄、影片逐幀渲染）、
  **網路**（下載、Gemini、TTS）、**磁碟空間**，以及**隱私**（逐字稿會送到 Google，見下方說明）。

> 價格、額度與條款會變動。本文中的外部數字查詢於 **2026-09-20**，請以官方頁面為準；
> 實測數字來自作者的 Windows 機器，僅供量級參考。

| 步驟 | 工具 | 現金 | 主要的非現金成本 |
|---|---|---|---|
| Step1 下載音檔 | yt-dlp（本地） | $0 | 頻寬、YouTube 頻率限制 |
| Step2 轉逐字稿 | faster-whisper（本地 CPU） | $0 | 運算時間、首次下載模型約 140 MB |
| Step3 改寫 + 拆 Scene | Google Gemini API（免費層級） | $0 | 額度限制；**內容可能被 Google 用於改進產品** |
| Step3.5 Scene 驗證 | 純 Python | $0 | 無 |
| Step4 配音 | edge-tts（線上服務） | $0 | 需要連網；非官方介面，無服務保證 |
| Step4 畫面 / 影片 | PIL + numpy + moviepy（本地 CPU） | $0 | **渲染時間**（約為影片長度的 2～3 倍）、磁碟空間 |

---

## 各步驟的成本設計

### Step1：下載音檔

- 只下載音軌（`bestaudio`）並轉成 128 kbps mp3，不下載影像，減少頻寬與轉檔時間
- 以 `video_id` 為 key 快取：音檔已存在就不再請求，避免對同一支影片重複下載

### Step2：轉逐字稿

- 使用本地 `faster-whisper`（預設 `base` 模型，CPU、int8），沒有 API 呼叫、沒有額度限制、不上傳音檔
- 代價是運算時間與辨識準確度（`base` 對中文偶有錯字，可用 `WHISPER_MODEL_SIZE` 換更大的模型）
- **長音檔前處理**：音檔超過 `MAX_AUDIO_MINUTES`（預設 15 分鐘）時，先用 ffmpeg `silenceremove`
  濾掉靜音再轉錄。本地運算的「成本」是時間與電力，縮短輸入同樣有價值；這個機制若日後改用
  按時數計費的雲端轉錄服務也能直接沿用。若素材本身幾乎沒有靜音，效果有限

### Step3：Gemini 改寫 + 拆 Scene——控制 LLM 成本的重點

1. **整份逐字稿只呼叫 1 次**。一次呼叫完成「切分主題 → 評分 → 改寫 → 拆成 Scene 並抓取圖表資料」，
   API 呼叫次數是 1 次，而不是「每個段落各 1 次」。Scene 的驗證、編號、補結尾都在本地 Python 完成，不再回頭問 LLM
2. **評分篩選**：Gemini 為每個候選段落打 1–10 分，程式依分數排序，只取前
   `MAX_SEGMENTS_TO_GENERATE`（預設 3）段進入 Step4；沒被選中的段落**不會**產生語音或影片
3. **實測用量**（一支約 15 分鐘的素材）：

   | 項目 | 大小 |
   |---|---|
   | 逐字稿 | 約 3,800 字 |
   | Prompt 樣板（Scene 規則與輸出格式說明） | 約 2,200 字 |
   | Gemini 回傳（篩選後的 JSON） | 約 2,100 字元（3 個段落、共 11 個 Scene） |

   一支影片就是 1 次請求、量級為數千 token，對請求速率限制的壓力很小。

**誠實揭露**：篩選機制只有在「候選段落數 > `MAX_SEGMENTS_TO_GENERATE`」時才會真正刷掉低分段落。
15 分鐘的素材 Gemini 通常只切出 2–3 段，剛好落在上限附近，所以多半沒有段落被刷掉；素材更長、
主題更多元時才會發揮作用。另外 LLM 輸出有隨機性，同一份逐字稿多次執行可能切出不同數量的段落。

**Gemini 免費層級與付費價格（參考）**

- 依 Google 官方定價頁（2026-09-20 查詢），`Gemini 3.5 Flash-Lite` 提供免費層級，價格欄標示為「Free of charge」
  （原先使用的 `Gemini 2.5 Flash-Lite` 已於近期對新用戶下架，本專案已切換至 3.5 版）；
  該頁沒有列出免費層級的具體請求速率限制，也沒有明確說明是否需要綁定付款方式，請以
  [Google AI Studio](https://aistudio.google.com/apikey) 與[官方定價頁](https://ai.google.dev/gemini-api/docs/pricing)為準
- **隱私**：同一頁標示免費層級的內容「用於改進 Google 產品（Used to improve our products：Yes）」。
  代表送出的逐字稿可能被 Google 使用——**不要用免費層級處理機密或含個資的內容**
- **若改用付費層級**：實際單價請以 Google 官方定價頁當下顯示為準（模型版本可能持續更新），
  依先前查詢的 Flash-Lite 系列定價量級（每百萬 token 輸入/輸出約在 $0.1~$0.4 區間）估算，
  單支影片的 Gemini 費用約為**千分之幾美元**量級。這是估算值，不是實際帳單

### Step4：配音、畫面與影片

- **只對入選的段落生成**：語音與影片都只針對 Step3 篩選後的段落
- **配音**：edge-tts 不需要 API key、沒有計費。每個 Scene 一次請求（上述範例是 11 次），
  Scene 的時長直接取自實際音檔長度，不需要額外的音訊分析。它是微軟 Edge 朗讀功能的非官方介面，
  沒有服務保證，日後可能改變或失效
- **畫面**：由 PIL + numpy 逐幀繪製，沒有外部素材、沒有影像生成 API。
  字型使用系統已安裝的中文字型
- **Scene 時長 = TTS 實際長度**：不讓 Gemini 估秒數，不會出現畫面比語音長或短的浪費
- **轉場與字幕都是本地運算**：轉場不重疊、不改長度；字幕文字直接沿用旁白，不需要額外的 API

**渲染時間（實測，作者機器，CPU 渲染，TTS 已快取）**

| 影片長度 | 渲染時間 |
|---|---|
| 30.1 秒 | 約 82 秒 |
| 36.9 秒 | 約 99 秒 |
| 44.5 秒 | 約 100 秒 |

約為影片長度的 2～3 倍。字幕使用逐幀合成，是渲染時間的主要來源之一。這是**時間成本，不是金錢成本**；
渲染結果有快取，不改內容就不會重跑。

---

## 快取：避免重複的時間、額度與網路成本

以 `video_id` 為 key，中間產物存放在 `data/cache/{video_id}/`，每個步驟執行前先檢查輸出是否存在：

- 已存在 → 印出「命中快取，跳過」，不重新執行
- 不存在 → 執行並寫入快取

| 快取內容 | 省下什麼 |
|---|---|
| `audio.mp3` | 重複下載與頻率限制 |
| `transcript.json` | 重複轉錄（最耗本機時間的一步） |
| `script_candidates.json`、`selected_scripts.json` | 重複的 Gemini 請求與額度消耗 |
| `segment_{i}_scene_{NN}_audio.mp3` | 重複的 TTS 請求 |
| `segment_{i}.mp4` | 重複渲染 |

即使全部改用免費工具，快取仍然有價值：本地運算消耗時間與電力；Gemini 免費層級有用量限制，
快取避免無謂地逼近上限；日後若換成付費服務，快取機制也能直接沿用。

> 注意：語音快取的檔名只含段落與 Scene 編號。重新讓 Gemini 改寫後，舊語音可能被誤用，
> 需一併刪除 `segment_*_scene_*_audio.mp3`（詳見 [README](./README.md#快取與重跑)）。

---

## 磁碟空間（實測，一支約 15 分鐘的素材）

| 項目 | 大小 |
|---|---|
| Whisper `base` 模型（首次下載，存在使用者的模型快取，不在專案內） | 約 140 MB |
| 該影片的音檔 `audio.mp3` | 約 14.5 MB |
| 該影片的整個快取資料夾（含音檔、逐字稿、腳本、語音、影片；其中包含開發過程中留下的暫存圖片） | 約 35 MB |

`data/cache/*/` 已加入 `.gitignore`，不會被推上 GitHub。

---

## 取捨與限制

- **字幕時間是估計值**：依標點與字數比例分配。若要逐字精確同步，需要解析 edge-tts 的
  語音邊界事件並修改 `tts.py`，投入與收益不成比例，因此目前採近似方式
- **付費服務的取捨**：主流 API 多數需要綁定付款方式，且免費層級常有額度、浮水印或資料使用上的限制。
  本專案優先選擇「沒有現金花費」的組合，換來的是較長的本機運算時間，以及對免費服務條款的依賴
- **未整合外部 AI 影片生成 / Avatar 服務**：這類服務的免費層級多半有浮水印或嚴格額度，
  與「不產生現金花費」的目標不符，因此未採用；畫面全部由程式繪製
- **對非官方服務的依賴**：edge-tts 沒有服務保證；`google.generativeai` 套件已被官方標示為
  deprecated，日後建議遷移到新版 SDK（`google.genai`）

---

_最後更新：2026-09-20。外部價格與條款查詢日期：2026-09-20。_
